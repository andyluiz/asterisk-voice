[general]
static=yes
writeprotect=yes
clearglobalvars=no

[internal]
exten => 1001,1,NoOp(Dialing local endpoint 1001)
 same => n,Dial(PJSIP/1001,30)
 same => n,Hangup()

exten => 1002,1,NoOp(Dialing local endpoint 1002)
 same => n,Dial(PJSIP/1002,30)
 same => n,Hangup()

exten => 600,1,NoOp(Echo test)
 same => n,Answer()
 same => n,Echo()
 same => n,Hangup()

exten => 700,1,NoOp(OpenClaw realtime inbound)
 same => n,Answer()
 same => n,Stasis(openclaw,inbound-realtime)
 same => n,Hangup()

exten => 9000,1,NoOp(OpenClaw ARI Stasis test)
 same => n,Answer()
 same => n,Stasis(openclaw,test)
 same => n,Hangup()
